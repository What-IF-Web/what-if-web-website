{
  "name": "what-if-web-website",
  "version": "1.0.0",
  "description": "Custom code for the What IF Web Webflow website.",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve app.js game.js",
    "build": "parcel build app.js --dist-dir",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.13.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
